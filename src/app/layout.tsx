import React, { ReactNode } from "react";
import "../styles/globals.css"; // 글로벌 스타일 시트

interface RootLayoutProps {
  children: ReactNode;
}

const RootLayout = ({ children }: RootLayoutProps) => {
  return (
    <html className="bg-#1c1c1f text-white w-full h-full">
      <head>
        <title>My Next.js App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </head>
      <body className="flex flex-col min-h-screen">
        <nav className="flex justify-between bg-#1c1c1f h-11 w-full">
          <ul className="flex flex-row space-x-4">
            <li className="flex justify-center items-center p-4 font-bold">
              <a href="/">YOUR.PUBG</a>
            </li>
            <li className="flex justify-center items-center">
              <a href="/about">About</a>
            </li>
            <li className="flex justify-center items-center">
              <a href="/blog">Blog</a>
            </li>
            <li className="flex justify-center items-center">
              <a href="/contact">Contact</a>
            </li>
          </ul>
        </nav>
        <main className="flex-grow">{children}</main>
      </body>
    </html>
  );
};

export default RootLayout;
